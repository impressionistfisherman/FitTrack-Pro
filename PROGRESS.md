# PROGRESS

## 2026-06-29 15:06:46 +09:00

### 작업 요약

- 식단 목표의 자동 칼로리 계산 기능이 UI에서 보이지 않는 문제를 수정함.
- 최근 식사 재기록 기능을 별도 카드가 아니라 식단 기록 흐름 안에서 참조하도록 재배치함.

### 변경 사항

- `client/src/pages/Meals.tsx`
  - `식단 목표` 설명을 수정해 저장 위치와 자동 계산/직접 수정 흐름을 명확히 표시함.
  - `칼로리 자동 계산` 블록을 추가하고 BMR, TDEE, 운동 목표, 주간 운동 횟수, 추천 전략을 표시함.
  - 추천값 적용 버튼 문구를 `계산값 적용`으로 변경함.
  - `최근 식사 다시 기록` 독립 카드를 제거함.
  - `식사 추가` 카드 내부에 `이전 기록 참조` 섹션을 추가함.
  - 이전 기록 참조 선택 시 기존 재기록 동작은 유지함.

- `TEST_RESULT.md`
  - 검증 결과 갱신.

- `PROGRESS.md`
  - 작업 상태 갱신.

### 현재 상태

- 필수 검증 통과
  - `.\node_modules\.bin\pnpm.CMD run check`
  - `.\node_modules\.bin\pnpm.CMD run test`
  - `.\node_modules\.bin\pnpm.CMD run build`
- 기존 dirty 파일인 `SESSION_HANDOFF.md`, `local-db/fittrack_local.sqlite*`는 작업 범위에서 제외해야 함.

### 남은 문제

- `이전 기록 참조`가 아직 입력 폼으로 불러오는 방식은 아님. 현재는 클릭 즉시 현재 날짜에 다시 기록함.
- 체중/운동 목표 데이터가 부족하면 자동 계산은 기본값 기반으로 동작함.
- 실제 계정 기반 브라우저 end-to-end 확인은 아직 별도 수행하지 않음.

### 다음 작업

- `이전 기록 참조`를 클릭 즉시 저장이 아니라 식단 입력 폼으로 불러와 수정 후 저장하는 방식으로 개선.
- 목표 칼로리 계산에 사용자 프로필 입력값 키, 활동량, 목표 기간을 추가.
- AI 이미지 인식 결과를 기존 음식 DB와 자동 매칭.
