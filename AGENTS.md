# FitTrack Pro 작업 규칙

이 저장소에서 기능을 추가하거나 UI를 수정할 때는 아래 순서를 지킨다.

## 필수 검증

푸시하기 전 다음 명령을 모두 실행한다.

```bash
pnpm run check
pnpm run test
pnpm run build
```

실패하면 원인을 수정한 뒤 같은 검증을 다시 실행한다. 검증이 끝나기 전에는 커밋하거나 푸시하지 않는다.

## 화면 동작 확인

기능 변경 후에는 관련 화면을 실제 사용자 흐름 기준으로 확인한다.

- 로그인/새로고침 후 이전 화면이 잠깐 보이지 않는지 확인
- 사용자 홈, 트레이너 홈, 관리자 홈 전환 확인
- 사이드바 항목이 중복되거나 증식하지 않는지 확인
- 모바일 또는 좁은 화면에서 버튼, 카드, 모달이 잘리지 않는지 확인
- 생성/수정/삭제 후 목록과 상세 화면이 즉시 갱신되는지 확인
- 로딩, 빈 상태, 에러 상태가 깨지지 않는지 확인

## 사이드바/라우팅 규칙

- 사이드바 항목은 `id`를 반드시 가진다.
- React key로 `href`만 쓰지 않는다. 같은 화면 내 anchor가 있으면 `href`가 겹치기 쉽다.
- 같은 뷰에 중복 `href`를 가진 메뉴를 여러 개 넣지 않는다.
- 역할별 화면은 사용자, 트레이너, 관리자 사이드바를 명확히 분리한다.
- 관리자/트레이너/사용자 홈 전환 UI는 권한이 있는 사용자에게만 표시한다.

## 커밋/푸시 규칙

- 작업과 무관한 파일은 스테이징하지 않는다.
- 로컬 DB 임시 파일과 Render 실험 파일은 커밋하지 않는다.
- 커밋 전 `git diff --check`로 공백 오류를 확인한다.
- 최종 응답에는 검증 결과와 커밋 해시를 포함한다.

## 자동 배포 규칙

- 코드, 설정, 문서 변경 작업이 완료되고 필수 검증이 통과하면 별도 요청을 기다리지 않고 관련 파일만 커밋하고 원격 저장소에 push한다.
- Render 운영 배포가 `master` 브랜치를 추적하므로 검증된 작업 브랜치를 `master`에 반영하고 push하여 자동 배포를 시작한다.
- 사용자가 명시적으로 커밋, push 또는 배포를 금지한 경우에만 자동 배포를 생략한다.
- 기존 사용자 변경사항, 로컬 DB 파일, 작업과 무관한 파일은 자동 배포 커밋에 포함하지 않는다.
- 배포 후 원격 `master` 커밋과 배포 상태를 확인하고 최종 응답에 결과를 기록한다.

## DB 반영 규칙

- 로컬 SQLite에서 동작을 확인하더라도 실제 사용 중인 운영 DB(Supabase Postgres)에도 반드시 같은 변경이 적용되도록 구현한다.
- DB 스키마, SQL 생성, 마이그레이션, 시드/동기화 로직을 바꿀 때는 SQLite 전용 성공에 그치지 않고 Postgres 경로도 함께 검증한다.
- 운영 DB에 적용되어야 하는 데이터 변경은 로컬 DB 파일 수정만으로 끝내지 말고, 코드/마이그레이션/동기화 스크립트 등 배포 가능한 형태로 남긴다.

## 우선순위

1. 기존 기능 안정화
2. 데이터 정합성
3. 로딩/빈 상태/에러 상태
4. 모바일/작은 화면 UX
5. 새 기능
